# Acceptance matrix and implementation backlog

Owner follow-up, 2026-09-23: 0.1.26 is published. The 0.1.27 branch adds native
access selection and recovery of preparations after removing their connection
or workspace. [Real native trials](REAL-AGENT-ACCEPTANCE-2026-09-23.md) establish
Codex/Claude editing, denial, cancellation and persisted native continuity;
Codex default-mode questions and Pi/OpenCode configured-model trials remain
open. [ADR 009](decisions/009-agent-access-and-extension-compatibility.md)
settles the owner's access, CLI-capability and VS Code compatibility directions.
An isolated VSCodium API probe passes; production extension integration is not
implemented. Native device/cloud and broader provenance gates remain open.

Current reconciliation, 2026-09-22: 0.1.25 is published; the 0.1.26 recovery
branch addresses the findings in [the audit](AUDIT-2026-09-22.md). Local indexed
search, source-line navigation with explicit rich-text limits, note moves,
Markdown backlinks and link rewriting, the terminal, generated graph modules
and bundled dependency notices are implemented. The older milestone paragraphs
below describe their original evidence, not missing features. Remaining work is
writable cloud capability/re-consent, real device/model acceptance, shared
source/run/artifact provenance and release readiness. New cloud preparations
now bind the exact account and shared drive. The host is Electron by ADR 005;
performance targets still need native measurement.

Source 0.1.5 continuation: [recovery and note tools](RECOVERY-AND-NOTE-TOOLS.md)
adds unsent/commit/conflict recovery, manual Windows update discovery,
image-resolution feedback, guarded note create/rename/move/trash/restore and
search selection for unambiguous rendered matches. These advance R01/R03/R04
and R08 without closing native installed-device, automatic updating, shared
provenance, backlink rewriting, full transcript management or cloud gates.
Older dated rows below retain their original scope; use the linked detail and
current STATUS for implemented boundaries.

Current D06 continuation, 2026-09-14: [local KB text search](KB-SEARCH.md) adds bounded literal search of saved Knowledge_Base text in one workspace KB, matching-line previews, guarded file opening and explicit incomplete results. It complements [source/artifact record navigation](KNOWLEDGE-NAVIGATION.md), metadata search, matching-version reconnection and version-specific reverse links. Since 2026-09-21 that search and the backlink list read a device-local index, so a request re-reads only the files that changed. Portable source/run/artifact identities, cross-KB/cloud search, properties, broad Markdown navigation and real generated-artifact acceptance remain open. Retained bytes and manually associated artifacts remain private to the device.

Ontology follow-up: [CSV tables and declared ontology graphs](ONTOLOGY.md) now support basic source editing, note links, parent/descendant and group filters, invalid-data feedback and a prepared native-agent setup request. Build, 64 behavior tests with all native controls enabled, all seven UI suites, Linux make/packaged CSV graph and website checks pass. The forced-renderer-crash shutdown regression also passes. Stable note/artifact/run identity, retained source versions and provenance remain D06 work. [Native CI 34752136969](https://github.com/DeL-TaiseiOzaki/irori/actions/runs/34752136969) passes all three package platforms, including the CSV graph, at implementation commit `8683900`.

Latest implementation: [independent workspace Drive ownership](WORKSPACE-DRIVE.md) advances R06/R07; empty workspaces and optional category selection work, and existing attachments remain intact. Native provider mounts/uploads, explicit legacy transfer, CSV/graph ontology and stable source/artifact provenance remain acceptance work. Local verification is build, 60 tests with native controls and six UI suites; hosted package evidence is tracked separately in [PACKAGING](PACKAGING.md).

Date: 2026-09-12. The first note/agent slice is implemented; no phase or full release is implicitly marked complete. Product source: KB_design/docs/irori/{SPEC,BUILD-PLAN,DECISIONS,IMPLEMENTATION-HANDOFF}. Those documents describe the entire intended product.

Current product decisions, 2026-09-13: [ADR 002](decisions/002-release-and-workspace.md) confirms MIT, Windows 11 x64/MacBook M5 Pro arm64, agent-built ontology with CSV editing/note links/hierarchy/subgraph visualization, and multiple independent GitHub repositories plus selected Drive folders in each workspace. It supersedes earlier Q01/Q02 uncertainty and mandatory KB ownership of Drive. Preserve existing data during that migration. [Forge packaging](PACKAGING.md) advances R01 but does not close native installer, signing, performance or clean-device acceptance.

Priority update, 2026-09-13: multiple GitHub checkouts plus multiple selected cloud folders are the product's central requirement. Initial startup/account/folder onboarding and read-only rclone management are now implemented. OpenCode/Pi adapters are also implemented; native control and protocol/UI fixture evidence is recorded in [HARNESSES](HARNESSES.md). Real model-turn acceptance remains open. See [WORKSPACE-CONNECTIONS](WORKSPACE-CONNECTIONS.md) for the full journey and [CLOUD-SETUP](CLOUD-SETUP.md) for the implemented subset. Fixture/native-RC checks do not establish successful Google consent or native mounts.

Git follow-up, 2026-09-13: per-space status/diff/history, reviewed staging/commit/push, fetch/fast-forward, manual native merge conflict resolution and GitHub clone onboarding are implemented and exercised with actual disposable Git repositories/local bare remotes. See [GIT](GIT.md). This advances the Git portion of R06/R07/R12 without closing live-account/native-platform or cloud acceptance.

Confirmed mount naming requirement: users choose the actual folder name beneath `contents` for each selected cloud folder. R07 acceptance includes an editable name and final-path preview, Japanese/space-containing names, collision/occupied-path validation, and preservation of the chosen name across reconnects and Drive-side renames. The name is independent of provider folder identity and is saved in the portable declaration.

| Requirement                                                          | Current evidence                                                                                                                                                                                                                                                                                                                                                                | Remaining acceptance work                                                                                                                                                                                                                                                                                     |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R01 Windows/Mac installed app | Forge EXE/DMG/ZIP and relocated native Linux x64/Windows x64/Mac arm64 startup, SDK loading and note save pass in CI | Signed/notarized installers, Windows 11/Ryzen 9 and M5 Pro installed-device acceptance, supported minimum OS versions and upgrades |
| R02 Lightweight, responsive                                          | On-demand editor/SDK/CLI; local measurement published                                                                                                                                                                                                                                                                                                                           | Native latency/p95/total memory acceptance for the chosen Electron host; the historical Linux RSS sample exceeded the proposed budget                                                                                                                                                            |
| R03 Notion-like Markdown editing | Crepe rich text/table display/edit, source mode, no-op preservation, dirty conflicts; bounded local KB body search over a device-local index and result opening; following a note's own relative Markdown links, listing the notes that link to it, and rewriting relative Markdown links when a note is renamed or moved | Native Japanese IME, slash/block movement/table UX fixtures, undo/redo, broad dialect preservation, cross-KB search, exact line navigation, properties, rewriting of wiki/HTML references |
| R04 Native harnesses: Codex, Claude Code, OpenCode and Pi            | All four selectable; Codex/Claude real native-account note reads/edits; OpenCode/Pi native control probes and explicit protocol/UI fixtures; shared scoped session handles/reset; KB skills with role/project scopes, retirement markers and a per-harness reach check against sourced discovery tables ([SKILLS](SKILLS.md))                                                                                                                                                                                                | Codex real structured question/denial gates, native restart-resume acceptance, full transcript/history management beyond the bounded local display window, expired login recovery, native rules/skills/extensions/MCP parity by capability; OpenCode/Pi real model turns and native-model resume; supported-version failure UX; mounted-file acceptance per harness |
| R05 Optional terminal, terminal-free ordinary flow                   | Note/agent journeys need no shell entry; optional xterm/node-pty terminal and packaged shell/file/cleanup fixtures pass                                                                                                                                                                                                                                                                                                                                | Installed-device shell discovery, resize, Unicode, Ctrl-C and child cleanup on both OSes                                                                                                                                                                                                                               |
| R06 Personal + multiple teams + organization                         | Five-space isolation, categories, UUIDs; named startup selections with profile edit/removal and offline-ID retention; multiple existing-checkout registration/inspection; GitHub clone onboarding with native Git fixture; restart UI fixture                                                                                                                                   | Full offline space UI, remove/rebind UI, live GitHub clone acceptance, scale fixture                                                                                                                                                                                                                          |
| R07 GitHub + multiple Drive folders/shared drives (central priority) | Account/folder/shared-drive UI, user-chosen names and collisions, portable IDs/device account bindings, private rclone config/RC, read-only mount identity/reconnection code, attachment rename/removal and unused account removal; real RC and explicit UI fixtures pass; Git review/stage/commit/history/fetch/pull/merge/push/clone pass with native disposable repositories | Actual Google consent and native mounts, mounted-file harness access, write/upload/recovery lifecycle, moved-folder recovery; live GitHub login/expired credentials/branch protections; Windows/macOS Git and conflict acceptance, branch/rebase/binary-conflict workflows, independent Git/cloud recovery    |
| R08 Per-space ontology/notes/artifacts | Declared CSV table/graph mappings, hierarchy/subgraph filters and note links; device-local source/run/artifact IDs, record search, current-file and artifact navigation, explicit matching-version reconnection; bounded local KB text search with line previews and an on-demand backlink list, both over a device-local index; relative link navigation between notes | Portable shared identities/provenance, cross-KB/cloud search, exact line navigation, properties, wiki/HTML reference handling, large/native ontology acceptance |
| R09 Versioned many-to-many lineage | Retained saved-source bytes/SHA-256, immutable run observations, separately appended manual artifact registrations, exact-version reverse links and restore/reconnection across restart | Shared provenance export, provider-native versions, full history management, complete source selection and real generated-artifact/PPTX acceptance; manual registration is not proof of generation |
| R10 Arbitrary formats                                                | All entries navigable, bounded UTF-8 editor, explicit external-open confirmation; verified cloud-text read-only path implemented                                                                                                                                                                                                                                                | Native cloud-file acceptance, image/PDF preview, provider-native Docs links, external-open native-platform test                                                                                                                                                                                               |
| R11 irori-extention model                                                  | Ownership before classification; nested roots excluded; contents overrides; aliases rejected; folder ID/mount verification code and false-mount rejection test                                                                                                                                                                                                                  | Successful native mount acceptance, reference resolution states, scoped note/artifact identity, explicit curation with source provenance                                                                                                                                                                      |
| R12 Reuse                                                            | React/Electron/Crepe/CodeMirror/SDK/Chokidar/cross-spawn/tree-kill/Zod; native Git collaboration and external rclone service                                                                                                                                                                                                                                                    | Remaining Git workflows, real file-generation integration and release-channel redistribution review; generated notices and packaged dependency inventory are implemented                                                                                                                                                                                |

## Sequenced work after this slice

1. Verify the implemented startup/account/folder flow with an irori-configured Google OAuth application and native Windows/macOS/FUSE facilities. Exercise successful mounts, account expiry, failure/reconnection, and actual harness-visible paths. Workspace-owned Drive connections implement the decided Q02 separation for new attachments.
2. Extend read-only mounts with durable pending-write recovery and independently observed remote completion before enabling cloud writes. Test multiple real accounts and independent connection failures. Verify the implemented account/attachment lifecycle on native mounts; verify the implemented reviewed Git operations with live GitHub/native platforms and complete independent Git/cloud recovery.
3. Close native parity and actual model acceptance for all four adapters: real structured questions and denial where supported, resume/failure, rules/skills/extensions/MCP by native capability, GUI PATH and mounted-file reads/writes. Scope/checkout-bound session handles and reset are implemented; bounded display history and pending instructions now persist (CONVERSATIONS.md), while real-provider restart verification and full history management remain open.
4. Benchmark the chosen Electron host with the full editor/agent/mount workload. Keep shared `HostAPI` and reusable Node services; measure native latency and total memory against accepted budgets. Complete Japanese IME and native editor acceptance. ADR 005 settled the host; a Tauri rewrite is not pending work.
5. Complete file safety/scale: late external-write race strategy, merge/review, draft pruning, 15,118-file/five-space fixture, mount-aware invalidation, offline/remove/rebind UI, stable note IDs and vault mapping preview. Strengthen source fallback. Preserve the completed CSV/graph presentation, indexed search, note moves and backlinks; complete explicit selected-source context and the unsupported reference dialects.
6. Extend versioned output provenance; the optional native terminal is implemented. Produce a real PPTX from two observed note versions with an existing generation library/skill, retain every generation's source-version/run relationships, provide manual partial-evidence registration, and exercise cloud/Git failures independently.
7. Complete clean-device onboarding, signing/updating, accessibility, non-engineer task trial, provider distribution/auth review and backup/export documentation.

## Invariants retained for later phases

- Explicit space category and stable scoped IDs; paths locate and hashes version data.
- Nested declared scopes own themselves; contents registration forbidden even via realpath aliases, in either registration order.
- Scope rules are never hoisted or concatenated across spaces. Native ancestor discovery is not overridden.
- Declared contents always bypasses extension classification. Its directory contains only selected-folder attachments; ordinary local bytes there are an anomaly.
- Portable facts/lineage are tracked by the owning KB; paths, credentials, observations, full transcripts and caches are device-local.
- Clouds and Git are separate recoverable operations; pending bytes survive restart. Disconnection proves neither absence nor deletion.
- Every output version retains its producing run and source versions. Identical output hashes do not collapse distinct generation history.
- Cross-scope disclosure requires explicit selection; basic contents-to-note curation retains its source reference.

Q01 and Q02 are answered in ADR 002; older source requirements describe the preceding proposals. CSV/graph ontology remains mandatory for the full release.
